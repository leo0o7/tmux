# C-a as prefix
unbind C-b
set -g prefix C-a
bind C-a send-prefix

# colors
set -sg terminal-overrides ",*:RGB"
# kitty term
# set -g default-terminal "xterm-kitty"
set -g default-terminal "xterm-256color"
set -sa terminal-overrides ",xterm-256color:RGB"
# set -ga terminal-overrides ",xterm-256color:Tc"

# undercurl support
set -as terminal-overrides ',*:Smulx=\E[4::%p1%dm'
# underscore colors
set -as terminal-overrides ',*:Setulc=\E[58::2::%p1%{65536}%/%d::%p1%{256}%/%{255}%&%d::%p1%{255}%&%d%;m'

# image.nvim plugin
set -gq allow-passthrough on
set -g visual-activity off

# clipboard passthrough to nvim
set -s set-clipboard on

# vi mode for navigation
set -g mode-keys vi

# don't destory on detach
set -g detach-on-destroy off
# don't ask for confirmation on delete
bind x kill-pane

# clear term
bind C-l send-keys -R \; send-keys Enter

# non 0 based window index
set -g base-index 1
set -g pane-base-index 1
set-window-option -g pane-base-index 1
set-option -g renumber-windows on

# resize windows
bind j resize-pane -D 5
bind k resize-pane -U 5
bind l resize-pane -R 5
bind h resize-pane -L 5

# maximize panes with m
unbind z
bind m resize-pane -Z
# possible missclicks (can remove if M key is needed for something else)
bind M resize-pane -Z

# enable mouse for scrolling and resizing windows
set -g mouse on

# enter copymode with v
unbind v
bind v copy-mode

# don't exit copy mode when dragging with mouse
unbind -T copy-mode-vi MouseDragEnd1Pane

# start selecting text with "v"
bind-key -T copy-mode-vi 'v' send -X begin-selection
# copy text with "y"
bind-key -T copy-mode-vi 'y' send -X copy-selection

# exit copy mode with Escpae
bind-key    -T copy-mode    Escape            send-keys -X cancel

# quick script to switch sessions
#bind-key "f" run-shell "~/bin/tmux-session-switch.sh"
# sesh search
bind-key "f" run-shell "sesh connect \"$(
  sesh list --icons | fzf-tmux -p 55%,60% \
    --no-sort --ansi --border-label ' sesh ' --prompt '⚡  ' \
    --header '  ^a all ^t tmux ^g configs ^x zoxide ^d tmux kill ^f find' \
    --bind 'tab:down,btab:up' \
    --bind 'ctrl-a:change-prompt(⚡  )+reload(sesh list --icons)' \
    --bind 'ctrl-t:change-prompt(🪟  )+reload(sesh list -t --icons)' \
    --bind 'ctrl-g:change-prompt(⚙️  )+reload(sesh list -c --icons)' \
    --bind 'ctrl-x:change-prompt(📁  )+reload(sesh list -z --icons)' \
    --bind 'ctrl-f:change-prompt(🔎  )+reload(fd -H -d 2 -t d -E .Trash . ~)' \
    --bind 'ctrl-d:execute(tmux kill-session -t {2..})+change-prompt(⚡  )+reload(sesh list --icons)' \
)\""

# reload config
unbind r
bind r source ~/.config/tmux/tmux.conf \; display-message 'Reloaded tmux config'

# new split in cwd
bind-key - split-window -v -c "#{pane_current_path}"
bind-key | split-window -h -c "#{pane_current_path}"

set -g @plugin "tmux-plugins/tmux-sensible"
set -g @plugin "christoomey/vim-tmux-navigator"
# set -g @plugin "seebi/tmux-colors-solarized"
set -g @plugin 'tmux-plugins/tmux-resurrect'
set -g @plugin 'tmux-plugins/tmux-continuum'
set -g @plugin 'tmux-plugins/tmux-battery'
set -g @plugin 'tmux-plugins/tmux-cpu'

set -g @resurrect-capture-pane-contents 'on'
set -g @continuum-restore 'on'

# Use the Catppuccin statusline modules with the Rosé Pine Moon palette.
set -g @catppuccin_flavor "mocha"
set -g @thm_bg "default"
set -g @thm_fg "#e0def4"
set -g @thm_rosewater "#ea9a97"
set -g @thm_flamingo "#ea9a97"
set -g @thm_pink "#eb6f92"
set -g @thm_mauve "#c4a7e7"
set -g @thm_red "#eb6f92"
set -g @thm_maroon "#ea9a97"
set -g @thm_peach "#f6c177"
set -g @thm_yellow "#f6c177"
set -g @thm_green "#9ccfd8"
set -g @thm_teal "#9ccfd8"
set -g @thm_sky "#9ccfd8"
set -g @thm_sapphire "#3e8fb0"
set -g @thm_blue "#3e8fb0"
set -g @thm_lavender "#c4a7e7"
set -g @thm_subtext_1 "#908caa"
set -g @thm_subtext_0 "#6e6a86"
set -g @thm_overlay_2 "#56526e"
set -g @thm_overlay_1 "#44415a"
set -g @thm_overlay_0 "#393552"
set -g @thm_surface_2 "#393552"
set -g @thm_surface_1 "#2a273f"
set -g @thm_surface_0 "#2a273f"
set -g @thm_mantle "#2a283e"
set -g @thm_crust "#232136"

set -g @catppuccin_window_status_style "basic"
set -g @catppuccin_status_module_text_bg "#2a273f"
set -g @catppuccin_application_color "#ea9a97"
set -g @catppuccin_cpu_color "#f6c177"
set -g @catppuccin_session_color "#{?client_prefix,#eb6f92,#9ccfd8}"
set -g @catppuccin_uptime_color "#3e8fb0"
set -g @catppuccin_battery_color "#c4a7e7"
set -g @catppuccin_status_application_icon_bg "#ea9a97"
set -g @catppuccin_status_cpu_icon_bg "#f6c177"
set -g @catppuccin_status_session_icon_bg "#{?client_prefix,#eb6f92,#9ccfd8}"
set -g @catppuccin_status_uptime_icon_bg "#3e8fb0"
set -g @catppuccin_status_battery_icon_bg "#c4a7e7"
set -g @catppuccin_status_application_icon_fg "#232136"
set -g @catppuccin_status_cpu_icon_fg "#232136"
set -g @catppuccin_status_session_icon_fg "#232136"
set -g @catppuccin_status_uptime_icon_fg "#232136"
set -g @catppuccin_status_battery_icon_fg "#232136"
set -g @catppuccin_status_application_text_fg "#e0def4"
set -g @catppuccin_status_session_text_fg "#e0def4"
set -g @catppuccin_status_uptime_text_fg "#e0def4"
set -g @catppuccin_status_battery_text_fg "#e0def4"
set -g @catppuccin_status_application_text_bg "#2a273f"
set -g @catppuccin_status_session_text_bg "#2a273f"
set -g @catppuccin_status_uptime_text_bg "#2a273f"
set -g @catppuccin_status_battery_text_bg "#2a273f"
set -g @cpu_low_fg_color "#e0def4"
set -g @cpu_medium_fg_color "#e0def4"
set -g @cpu_high_fg_color "#232136"
set -g @cpu_low_bg_color "#2a273f"
set -g @cpu_medium_bg_color "#2a273f"
set -g @cpu_high_bg_color "#eb6f92"
set -g @catppuccin_window_number_color "#908caa"
set -g @catppuccin_window_text_color "#393552"
set -g @catppuccin_window_current_number_color "#c4a7e7"
set -g @catppuccin_window_current_text_color "#393552"

set -g @catppuccin_window_current_text " #{window_name}"
set -g @catppuccin_window_text " #{window_name}"
set -g @catppuccin_window_current_fill "all"

run ~/.config/tmux/plugins/catppuccin/tmux/catppuccin.tmux

set -g status-right-length 100
set -g status-left-length 100
set -g status-left ""
set -g status-right "#{E:@catppuccin_status_application}"
set -agF status-right "#{E:@catppuccin_status_cpu}"
set -ag status-right "#{E:@catppuccin_status_session}"
set -ag status-right "#{E:@catppuccin_status_uptime}"
set -agF status-right "#{E:@catppuccin_status_battery}"

run "~/.tmux/plugins/tpm/tpm"
